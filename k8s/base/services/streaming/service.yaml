apiVersion: v1
kind: Service
metadata:
  name: streaming
spec:
  selector:
    app: streaming
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
    - name: websocket
      port: 8080
      targetPort: 8080
  type: ClusterIP
